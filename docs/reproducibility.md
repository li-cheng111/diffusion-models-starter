# 可复现性规范

每次正式实验至少记录以下字段：项目名、配置文件、Git 提交、随机种子、数据集划分、
真实图和生成图数量、硬件、Python/PyTorch/CUDA 版本、checkpoint SHA256、输出 JSON
和图表路径。

所有 FID 对比必须固定真实数据划分、生成样本数、checkpoint、评估 seed 和输入范围。
不同 Project 的结果只有在这些字段一致或差异被报告明确说明时才允许放在同一张图中比较。
